// indexOf(), SIMILAR TO includes(), searches the array for the specified item, and returns its position.

// Search an array for the item 'Mango':
const fruits = ['Banana', 'Apple', 'Mango']

console.log(fruits.indexOf('Mango')) // 2
