// toString(), returns a string with all the array values, separated by commas.

const fruits = ['Banana', 'Apple', 'Mango']
console.log(fruits.toString()) // Banana,Apple,Mango

// join IS BETTER AT FORMATTING
console.log(fruits.join(', ')) // Banana, Apple, Mango

// This method will not change the original array.
