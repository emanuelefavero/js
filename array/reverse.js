// reverse(), reverses the order of the elements in an array.
const fruits = ['Banana', 'Apple', 'Mango']
console.log(fruits.reverse()) // [ 'Mango', 'Apple', 'Banana' ]

// this method will change the original array.
