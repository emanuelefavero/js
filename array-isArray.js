// Array.isArray() returns true if the argument is an array, and false if it is not

const nums = [1, 2, 3]
const text = 'Hello'

console.log(Array.isArray(nums)) // true
console.log(Array.isArray(text)) // false
