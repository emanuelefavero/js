// * Convert a number to binary
let num = 2
console.log(num.toString(2)) // 10

num = 10
console.log(num.toString(2)) // 1010

// TIP: By passing 2 as the radix argument, we can convert a number to binary

// * Convert binary to number
let binaryNum = '1010'
console.log(parseInt(binaryNum, 2)) // 10
