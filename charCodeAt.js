// charCodeAt - returns the unicode of the character at the specified index in a string

console.log('!'.charCodeAt(0)) // 33
console.log('A'.charCodeAt(0)) // 65
console.log('a'.charCodeAt(0)) // 97
console.log('€'.charCodeAt(0)) // 8364

// TIP: UNICODE is a standard that assigns a unique number to every character in every language
