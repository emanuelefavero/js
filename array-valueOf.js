// The valueOf method returns the array itself
let colors = ['red', 'green', 'blue']
console.log(colors.valueOf()) // [ 'red', 'green', 'blue' ]
console.log(colors) // [ 'red', 'green', 'blue' ]

// TIP: The valueOf() method does not change the original array.
