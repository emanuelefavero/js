// The Boolean function checks if an expression is true

console.log(Boolean(10 > 5)) // true
console.log(Boolean(10 > 20)) // false

// You can also omit the Boolean function and just check this way
console.log(10 > 5) // true
